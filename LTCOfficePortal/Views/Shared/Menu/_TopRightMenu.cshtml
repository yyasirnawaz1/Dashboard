@{
    Layout = null;
}

<ul class="navbar-nav">
    <li class="nav-item dropdown dropdown-user">
        <a href="#" class="navbar-nav-link d-flex align-items-center dropdown-toggle" data-toggle="dropdown">
            <i class="icon-user"></i>
            <span>@User.Identity.Name</span>
        </a>
        <div class="dropdown-menu dropdown-menu-right">
            <a href="#" data-toggle="modal" data-target="#userprofileModal" class="dropdown-item"><i class="icon-user-plus"></i> User Profile</a>
            <a href="#" data-toggle="modal" data-target="#changePasswordModal" class="dropdown-item"><i class="icon-envelope"></i> Change Password</a>
            <div class="dropdown-divider"></div>
            <a href="@Url.Action("Logout","Account")" class="dropdown-item"><i class="icon-users"></i> Logout</a>
            @*<div class="dropdown-divider"></div>
            <a href="#" data-toggle="modal" data-target="#aboutModal" class="dropdown-item"><i class="fa fa-tv"></i> About</a>*@
        </div>
    </li>
</ul>